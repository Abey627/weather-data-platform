FROM node:16-alpine

WORKDIR /app

# Install dependencies first (for better layer caching)
COPY package.json package-lock.json* ./
RUN npm install

# The rest of the code will be mounted as a volume
# This enables hot reloading during development

# Expose port
EXPOSE 3000

# Use this command for development with hot reloading
CMD ["npm", "start"]
